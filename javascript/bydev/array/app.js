var names = ['I','sujitha','married'];


// names.pop(); removes last
// names.push('sex'); adds to the last
// names.shift();  --removes first index
// names.unshift('New'); -- adds it to the first
// console.log(names.indexOf('sujitha')); -- find the index number

console.log(names);